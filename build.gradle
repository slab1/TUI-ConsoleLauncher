buildscript {
    repositories {
        google()
        mavenCentral()}
    dependencies {
        // CRITICAL: This must be 8.3.2 to work with Gradle 8.9
        classpath 'com.android.tools.build:gradle:8.3.2'
    }
}

task clean(type: Delete) {delete rootProject.buildDir
}